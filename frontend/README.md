# Frontend - Smart Sweep & Explain Engine

This directory contains the React/Vite frontend for the application.

To run it independently from the root `run.bat` script:

```bash
npm install
npm run dev
```

See the root `README.md` for complete project details and architecture overview.
